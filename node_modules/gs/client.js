MODULE_NAME = 'Gamesocket (Client)';


var log = function(msg) {
	console.log(MODULE_NAME + ': '+msg)
}
log('<-- Loading...');


var gs_models = require('gs/models');
var syncr = require('socketsyncr/client');

var syncClient = new syncr.SyncClient(HOST,{'port':PORT});
this.syncClient = syncClient; // <-- Make globally available
syncClient.connect()


log('Client connected.')